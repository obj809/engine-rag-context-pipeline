# Pinned to a known-good set. sentence-transformers/numpy match the indexer repo
# so query embeddings line up with the indexed document embeddings. torch is
# installed from the CPU-only index in the Dockerfile (pinned there).
langchain-core==1.4.6
langchain-openai==1.3.0
sentence-transformers==5.5.1
numpy==2.4.6
python-dotenv==1.2.2
psycopg[binary]==3.3.4
pgvector==0.4.2
